# Local virtual environment. Python 3.12.3.
#
#   python -m venv venv && ./venv/bin/pip install -r requirements.txt
#
# Not for Colab; the notebooks there install only what Colab lacks.
#
# The committed .pkl scalers were written with scikit-learn 1.6.1, so reading them
# under 1.7.0 emits a harmless InconsistentVersionWarning.

# Unpinned: PyPI's Linux wheel is CUDA-only, so ROCm and CPU users need another build.
torch>=2.2
# Integrates the learned vector field in flowMatching/model.py
torchdiffeq==0.2.5

scikit-learn==1.7.0
xgboost==2.1.4

numpy==1.26.4
pandas==2.2.3
scipy==1.15.2
pyarrow==24.0.0
joblib==1.5.1

umap-learn==0.5.11
matplotlib==3.10.1
seaborn==0.13.2

# Only needed by prepare_datasets.py, which reads the LUCAS shapefile
geopandas==1.1.2

# Notebook execution. nbconvert also allows running the notebook headlessly:
#   jupyter nbconvert --to notebook --execute --inplace notebooks/03_generate_paper_results.ipynb
ipykernel==7.1.0
nbconvert==7.16.6
